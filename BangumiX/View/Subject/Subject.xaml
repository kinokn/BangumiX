<UserControl
    x:Class="BangumiX.View.Subject"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BangumiX.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:common="using:BangumiX.Common"
    xmlns:vm="using:BangumiX.ViewModel"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="840">
    <UserControl.Resources>
        <common:NullVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <Style TargetType="TextBox">
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0 4 0 0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement"
                                               Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightChromeAltLowBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundElement"
                                               Storyboard.TargetProperty="Opacity">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundHoverOpacity}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Focused">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundElement"
                                               Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundChromeWhiteBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundElement"
                                               Storyboard.TargetProperty="Opacity">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundFocusedOpacity}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement"
                                               Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightAccentBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement"
                                               Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlForegroundChromeBlackHighBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement"
                                               Storyboard.TargetProperty="RequestedTheme">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Light" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="BackgroundElement"
                                    Background="{TemplateBinding Background}"
                                    Margin="{TemplateBinding BorderThickness}"
                                    Opacity="{ThemeResource TextControlBackgroundRestOpacity}"/>
                            <Border x:Name="BorderElement"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ScrollViewer x:Name="ContentElement"
                                          HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                          IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
                                          IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
                                          IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                                          Margin="{TemplateBinding BorderThickness}"
                                          Padding="2 -1 0 1"
                                          IsTabStop="False"
                                          AutomationProperties.AccessibilityView="Raw"
                                          ZoomMode="Disabled" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Name="GridMain" Width="840" Padding="10 42 10 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="350"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="225"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="{x:Bind subjectVM.ImageLarge, Mode=OneWay}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <StackPanel Margin="20 0 0 0" Grid.Column="1">
                <TextBlock HorizontalAlignment="Left" MaxWidth="400" FontSize="24" Text="{x:Bind subjectVM.Name, Mode=OneWay}" TextWrapping="Wrap" FontFamily="Yu Gothic" FontWeight="Bold"/>
                <TextBlock HorizontalAlignment="Left" MaxWidth="400" FontSize="20" Text="{x:Bind subjectVM.NameCN, Mode=OneWay}" TextWrapping="Wrap"/>
                <Grid Grid.Column="1" MaxHeight="200" Margin="0 10 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320"/>
                        <ColumnDefinition Width="25"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Width="320">
                        <TextBlock Margin="0 0 0 10" Text="观看进度管理：" FontSize="12" Foreground="Silver"/>
                        <Grid Margin="0 0 0 10" Height="50" Visibility="{x:Bind subjectVM.SubjectCollectStatus.StatusVisibility, Mode=OneWay}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind subjectVM.SubjectCollectStatus.Status, Mode=OneWay}"/>
                            <ProgressBar Grid.Row="1" Foreground="#FF004EB4" Value="{x:Bind subjectVM.SubjectCollectStatus.EpStatus, Mode=OneWay}" Minimum="0" Maximum="{x:Bind subjectVM.EpsCount, Mode=OneWay}"/>
                            <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" Orientation="Horizontal">
                                <TextBox Width="22" Height="18" VerticalContentAlignment="Top" Text="{x:Bind subjectVM.SubjectCollectStatus.ChangedEpStatus, Mode=TwoWay}"/>
                                <TextBlock Width="15" Text="/" TextAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Width="22" Height="16" Text="{x:Bind subjectVM.EpsCount, Mode=OneWay}"/>
                            </StackPanel>
                            <Button Grid.Row="1" Grid.Column="2" Height="18" Margin="0 0 1 -2" Padding="0" FontSize="12" HorizontalAlignment="Stretch" BorderThickness="1" Content="更新" Click="ProgressUpdateBtn_Click"/>
                        </Grid>
                        <GridView Padding="0 0 -8 0" x:Name="progressGridView" ItemsSource="{x:Bind subjectVM.EpsSub, Mode=OneWay}" SelectionChanged="GridView_SelectionChanged">
                            <GridView.ItemContainerStyle>
                                <Style TargetType="GridViewItem">
                                    <Setter Property="MinWidth" Value="18"/>
                                    <Setter Property="MinHeight" Value="18"/>
                                    <Setter Property="Background" Value="DimGray"/>
                                    <Setter Property="Margin" Value="0 0 7 7"/>
                                    <Setter Property="FontSize" Value="12"/>
                                </Style>
                            </GridView.ItemContainerStyle>
                            <GridView.ItemTemplate>
                                <DataTemplate x:DataType="vm:EpisodeViewModel">
                                    <Border MinWidth="20" MinHeight="20" BorderThickness="0 2 0 0" BorderBrush="{x:Bind EpStatusColor, Mode=OneWay}">
                                        <TextBlock HorizontalAlignment="Center" Text="{x:Bind Sort, Mode=OneWay}"/>
                                    </Border>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </StackPanel>
                    <Border Grid.Column="1" Width="1" Margin="0 25 0 0" HorizontalAlignment="Center" BorderBrush="DimGray" BorderThickness="0 0 1 0"/>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="评分：" FontSize="12" Foreground="Silver"/>
                        <RelativePanel Margin="0 0 0 20">
                            <TextBlock RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" Text="{x:Bind subjectVM.Score, Mode=OneWay}" FontSize="28"/>
                            <TextBlock RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignRightWithPanel="True" Text="{x:Bind subjectVM.TotalRatingString, Mode=OneWay}" FontSize="12" Foreground="Silver"/>
                        </RelativePanel>
                        <ItemsControl ItemsSource="{x:Bind subjectVM.RatingCount}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Margin="-6 0 0 0" Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:SubjectViewModel">
                                    <StackPanel VerticalAlignment="Bottom">
                                        <Border Margin="6 2" Background="DimGray" Width="12" Height="{Binding Value}"/>
                                        <Border Width="12">
                                            <TextBlock HorizontalAlignment="Center" FontSize="12" Foreground="GhostWhite" Text="{Binding Key}">
                                                <TextBlock.RenderTransform>
                                                    <TranslateTransform Y="0"/>
                                                </TextBlock.RenderTransform>
                                            </TextBlock>
                                        </Border>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
        <ContentControl x:Name="SubjectContentCtrl" Grid.Row="3"/>
        <ListView x:Name="subjectDetailListView" Grid.Row="1" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Center" SelectedIndex="0" SelectionChanged="ListView_SelectionChanged">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="FontSize" Value="18"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Items>
                <ListViewItem Content="概览" Tag="Summary"/>
                <ListViewItem Content="章节" Tag="Episode"/>
                <ListViewItem Content="角色" Tag="Character"/>
                <ListViewItem Content="制作" Tag="Staff"/>
                <ListViewItem Content="吐槽" Tag="Comment"/>
            </ListView.Items>
        </ListView>
        <Line Grid.Row="2" StrokeThickness="2" Stroke="DimGray" X1="0" X2="820"/>
    </Grid>
</UserControl>
